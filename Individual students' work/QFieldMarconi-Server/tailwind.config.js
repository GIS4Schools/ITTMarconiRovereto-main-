module.exports = {
    darkMode: 'media',
    content: ["./static/**/*.{html,js}", "./views/**/*.hbs"],
    theme: {
        extend: {},
    },
    plugins: [
        require('flowbite/plugin')
    ],
}
